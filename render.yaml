services:
  - type: web
    name: launch-preflight-checker
    runtime: docker
    rootDir: launch-preflight-checker
    dockerfilePath: ./Dockerfile
    healthCheckPath: /health
    autoDeploy: false
